<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:AutoSettingUI.WPF.Controls"
                    xmlns:models="clr-namespace:AutoSettingUI.Core.Models;assembly=AutoSettingUI.Core">

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    <local:BooleanToFontWeightConverter x:Key="BooleanToFontWeightConverter"/>

    <!-- Hierarchical DataTemplate for Navigation Tree -->
    <HierarchicalDataTemplate x:Key="NavigationNodeTemplate"
                              DataType="{x:Type models:NavigationNode}"
                              ItemsSource="{Binding Children}">
        <TextBlock Text="{Binding Title}"
                   FontWeight="{Binding IsMainHeader, Converter={StaticResource BooleanToFontWeightConverter}}"
                   Padding="2"/>
    </HierarchicalDataTemplate>

    <Style TargetType="{x:Type local:WpfAutoSettingPanel}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:WpfAutoSettingPanel}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="{TemplateBinding NavigationWidth}"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Navigation Panel -->
                        <Border Grid.Column="0"
                                Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                                BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                                BorderThickness="0,0,1,0"
                                Visibility="{TemplateBinding ShowNavigation, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <TreeView x:Name="PART_Navigation"
                                          Margin="5"
                                          BorderThickness="0"
                                          Background="Transparent"
                                          ItemTemplate="{StaticResource NavigationNodeTemplate}">
                                    <TreeView.ItemContainerStyle>
                                        <Style TargetType="TreeViewItem">
                                            <Setter Property="IsExpanded" Value="{Binding IsExpanded}"/>
                                            <Setter Property="IsSelected" Value="{Binding IsSelected}"/>
                                        </Style>
                                    </TreeView.ItemContainerStyle>
                                </TreeView>
                            </ScrollViewer>
                        </Border>

                        <!-- Form Panel -->
                        <ScrollViewer x:Name="PART_FormScrollViewer"
                                      Grid.Column="1"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                            <ContentControl Content="{TemplateBinding Targets}"/>
                        </ScrollViewer>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
